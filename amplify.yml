version: 1

frontend:
  phases:
    build:
      commands:
        - echo "No build required for static HTML/CSS/JS site"

  artifacts:
    baseDirectory: .
    files:
      - '**/*'

  cache:
    paths: []
