import { RECOMMENDATION_COPY, EVIDENCE_LABELS } from '../../content/recommendation-copy.js';
import { loadState, resetState } from '../../application/state.js';
import { resolveAssessment } from '../../domain/assessment-engine.js';
import { navigate } from '../../application/router.js';
const order=['setup','transition','decisionStyle','marketPsychology','evolution','tradeoff','age','goals'];
const list=items=>items.map(x=>`<div class="summary-item">${x}</div>`).join('');
export function renderRecommendation(root){document.title='AaronBux - Your Investing System';const state=loadState();if(!state.answers||!Object.keys(state.answers).length){root.innerHTML=`<div class="app-shell"><header class="topbar"><div class="topbar-inner"><button class="btn btn-secondary" id="backBtn">Back</button><div style="text-align:center"><div class="brand">AaronBux</div><div class="step-label">Your investing system</div></div><button class="btn btn-secondary" id="restartBtn">Restart</button></div><div class="progress-track"><div class="progress-fill" style="width:100%"></div></div></header><main class="main"><div class="card panel"><h2>We could not find your answers.</h2></div></main></div>`;bind();return;}const r=state.result||resolveAssessment(state),v=RECOMMENDATION_COPY[r.archetypeId]||RECOMMENDATION_COPY.GD;root.innerHTML=`<div class="app-shell recommendation-page"><header class="topbar"><div class="topbar-inner"><button class="btn btn-secondary" id="backBtn">Back</button><div style="text-align:center"><div class="brand">AaronBux</div><div class="step-label">Your investing system</div></div><button class="btn btn-secondary" id="restartBtn">Restart</button></div><div class="progress-track"><div class="progress-fill" style="width:100%"></div></div></header><main class="main"><section><div class="card panel result-hero"><span class="pill">The pattern in your answers</span><h1>${v.diagnosis}</h1><p class="lead">${v.need}</p><div class="confidence-line"><span>${r.confidence}% consistency across your answers</span><div class="bar"><div style="width:${r.confidence}%"></div></div></div></div><section class="card panel recommendation-section"><span class="pill">What needs to become clearer</span><h2>${v.gap}</h2><div class="summary-list">${list(v.gaps)}</div></section><section class="card panel recommendation-section"><span class="pill">What deserves your attention</span><h2>Different decisions need different levels of oversight</h2><div class="summary-list">${list(v.attention)}</div></section><section class="card panel recommendation-section"><span class="pill">Your review rhythm</span><h2>${v.rhythm}</h2><div class="summary-list">${list(v.rhythmItems)}</div></section><section class="card panel recommendation-section"><span class="pill">How the parts should work together</span><h2>${v.structure}</h2><div class="summary-list">${list(v.parts)}</div></section><section class="card panel recommendation-section"><span class="pill">When a change deserves consideration</span><div class="summary-list">${list(v.rules)}</div></section><section class="card panel recommendation-section"><span class="pill">How your answers shaped this</span><div class="summary-list">${order.filter(k=>state.answers?.[k]?.length).map(k=>`<div class="summary-item"><strong>${EVIDENCE_LABELS[k]}</strong><br>${state.answers[k].map(a=>a.label).join(' · ')}</div>`).join('')}</div></section></section></main></div>`;bind();function bind(){document.getElementById('backBtn').onclick=()=>navigate('assessment/8');document.getElementById('restartBtn').onclick=()=>{resetState();navigate('');};}}
